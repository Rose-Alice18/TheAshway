# ============================================
# Perpway Backend Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit the .env file to version control!

# ============================================
# DATABASE CONFIGURATION
# ============================================

# MongoDB Connection String
# Local MongoDB: mongodb://localhost:27017/perpway
# MongoDB Atlas (Cloud): mongodb+srv://username:password@cluster.mongodb.net/perpway
MONGODB_URI=mongodb://localhost:27017/perpway

# ============================================
# SERVER CONFIGURATION
# ============================================

# Port for backend server (default: 5000)
PORT=5000

# Frontend URL (for CORS configuration)
FRONTEND_URL=http://localhost:2000

# Node environment (development or production)
NODE_ENV=development

# ============================================
# EMAIL CONFIGURATION (Gmail)
# ============================================
# Email notifications will be sent to roselinetsatsu@gmail.com when customers create delivery requests

# Your Gmail address (the account that will SEND the notification emails)
EMAIL_USER=your-email@gmail.com

# Gmail App Password (NOT your regular Gmail password!)
#
# HOW TO GET A GMAIL APP PASSWORD:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification (if not already enabled)
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Select "Mail" and your device type
# 5. Google will generate a 16-character password (example: abcd efgh ijkl mnop)
# 6. Copy the password (without spaces) and paste it below
# 7. NEVER use your regular Gmail password here - it won't work!
EMAIL_PASS=your-16-character-app-password

# Admin email - Where delivery notifications will be sent
# This email will receive notifications whenever a customer creates a delivery request
ADMIN_EMAIL=roselinetsatsu@gmail.com

# ============================================
# PAYMENT GATEWAY (Optional - For Production)
# ============================================

# Paystack (Ghana)
PAYSTACK_SECRET_KEY=your-paystack-secret-key
PAYSTACK_PUBLIC_KEY=your-paystack-public-key

# Mobile Money (MTN/Vodafone Ghana)
MOMO_API_KEY=your-momo-api-key
MOMO_API_SECRET=your-momo-api-secret

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Copy this file to create your actual .env file:
#    cp .env.example .env
#
# 2. Edit the .env file and fill in your real values
#
# 3. IMPORTANT: Make sure .env is in your .gitignore file!
#    The .env file should NEVER be committed to version control
#
# 4. Restart your backend server after making changes:
#    cd backend
#    node server.js
#
# 5. Test email notifications:
#    - Go to http://localhost:2000/delivery
#    - Create a test delivery request
#    - Check roselinetsatsu@gmail.com for the notification email
#    - Check backend console logs for email sending status
#
# ============================================
# TROUBLESHOOTING
# ============================================
#
# Email not sending?
# ✓ Make sure 2-Step Verification is enabled on your Gmail
# ✓ Use an App Password, NOT your regular Gmail password
# ✓ Copy the App Password without spaces
# ✓ Check backend console for error messages
# ✓ Verify EMAIL_USER is a valid Gmail address
#
# MongoDB connection failing?
# ✓ For local: Make sure MongoDB service is running
# ✓ For Atlas: Check your IP is whitelisted in Atlas dashboard
# ✓ For Atlas: Verify username/password in connection string
# ✓ Check MONGODB_URI format is correct
#
# Need help?
# - Gmail App Passwords: https://support.google.com/accounts/answer/185833
# - MongoDB Atlas: https://www.mongodb.com/docs/atlas/
# - Backend logs: Check the terminal where you run "node server.js"
